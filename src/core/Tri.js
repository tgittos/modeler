//A tri is a type of face with 3 vertices.
//It has vertices, and maybe later, a material and a normal, etc
MODELER.Tri = function(params, my){
  //Private vars
  var that = {}, my = my || {},
  vertices = params.vertices || $V([0, 0, 0]);
  //Private functions
  //Public functions
  that.getVertices = function() {
    
  };
  that.getVerticesAsArray = function() {
    
  };
  return that; 
};